{
  "name": "nothing-cli",
  "version": "0.1.0",
  "description": "Node.js wrapper for the Nothing cryptographic bearer instrument",
  "bin": {
    "nothing": "./index.js"
  },
  "scripts": {
    "build-core": "cd ../nothing-core && cargo build --release",
    "start": "node index.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
